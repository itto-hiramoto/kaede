pub enum Option<T> {
    Some(T),
    None,
}

impl<T> Option<T> {
    pub fn unwrap(self): T {
        return match self {
            Option::Some(value) => value,
            Option::None => panic("called `Option::unwrap()` on a `None` value"),
        }
    }
}
